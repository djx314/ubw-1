sudo: true
language: scala

scala:
  - 2.11.8

jdk:
  - oraclejdk8

script:
  - sbt ++$TRAVIS_SCALA_VERSION allClean coverage allTest
  - sbt ++$TRAVIS_SCALA_VERSION allReport

after_success: bash <(curl -s https://codecov.io/bash)